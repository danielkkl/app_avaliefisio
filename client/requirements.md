## Packages
framer-motion | Page transitions and complex animations
recharts | Dashboard analytics charts and data visualization
date-fns | Date formatting for evaluation dates
react-hook-form | Form state management
@hookform/resolvers | Zod integration for forms
zod | Schema validation

## Notes
Authentication is handled via Replit Auth.
API routes are defined in @shared/routes.
Fichas (evaluations) have a complex schema, so forms will be broken down into tabs.
